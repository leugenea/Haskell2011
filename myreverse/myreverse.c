#define BUFF_SIZE 10

typedef struct
{
  int size;
  char* chars;
} Buffer;

Buffer* newBuff
{
  buff* = malloc(sizeof);
  buff->chars = malloc(BUFF_SIZE);
  buff->size = 0;
  
  return buff;
}

int main()
{
  
}